import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    marginTop: 30,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  instantCard: {
    borderWidth: 1,
    alignItems: 'center',
  },
  scheduledCard: {
    borderWidth: 1,
    alignItems: 'center',
  },
});
